FROM node:24-slim

WORKDIR /app

COPY package*.json ./
RUN npm install --omit=dev --omit=optional

COPY . .

ENV NODE_ENV=production
ENV APP_MODE=official
ENV SCRAPER_ENABLED=false
ENV PORT=3000

EXPOSE 3000

CMD ["node", "src/server.js"]
