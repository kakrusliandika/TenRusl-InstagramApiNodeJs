version: 1.0
runtime: docker
build:
  commands:
    build:
      - docker build -t tenrusl-instagram-api .
run:
  runtime-version: latest
  command: npm start
  network:
    port: 3000
  env:
    - name: NODE_ENV
      value: production
    - name: IG_PROVIDER
      value: mock
